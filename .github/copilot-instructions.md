# Copilot Instructions

When working on this project:

- Follow the conventions in AGENTS.md
- Use Mona/octocat theming in UI copy (e.g., "Great catch!" instead of "Correct!")
- Prefer accessibility: all interactive elements need aria labels
- Use semantic HTML elements over generic divs
- Keep bundle size small — avoid adding new dependencies without justification
